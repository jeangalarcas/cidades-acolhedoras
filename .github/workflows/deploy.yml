# Deploy via Vercel — workflow do GitHub Pages desativado
# O Vercel faz o deploy automaticamente a cada push no main
name: Deploy (desativado - usar Vercel)

on:
  workflow_dispatch:  # só roda se chamado manualmente

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy feito automaticamente pelo Vercel"